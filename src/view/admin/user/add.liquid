{% include "admin/inc/header" %}
{% include "admin/inc/nav" %}

  <div class="container">
    <div class="row">
      <div class="col-md-4 col-md-offset-4">
        <div class="signin-panel panel panel-default">
          <div class="panel-heading">
            <h3 class="panel-title">Add Application</h3>
          </div>
          <div class="panel-body">
            {% if error %}
            <div class="alert alert-danger">{{error|escape}}</div>
            {% endif %}
            <form role="form" method="post" enctype="multipart/form-data">
              <input type="hidden" name="_csrf" value="{{_csrf|escape}}">
              <div class="form-group">
                <label for="ipt-name">Name</label>
                <input type="text" class="form-control" id="ipt-name" placeholder="Application Name" name="name" value="{{_server.body.name|escape}}">
              </div>
              <div class="form-group">
                <label for="ipt-email">E-mail</label>
                <input type="email" class="form-control" id="ipt-email" placeholder="E-mail Address" name="email"value="{{_server.body.email|escape}}">
              </div>
              <div class="form-group">
                <label for="ipt-password">Password</label>
                <input type="password" class="form-control" id="ipt-password" placeholder="New Password" name="password" value="{{_server.body.password|escape}}">
              </div>
              <hr>
              <button type="submit" class="btn btn-success btn-block">Submit</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>

{% include "admin/inc/footer" %}
